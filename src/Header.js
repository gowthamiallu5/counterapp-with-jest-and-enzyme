import React from "react";
import "./App.css";

const Header = (props) => {
    return (
    <div className="headerComponent">
      <span className="spanheader">POC Header</span>
    </div>
  );
}

export default Header;
